# 阶段五：查询与报表确认基线

状态：用户已确认问题 1～3 均按 AI 建议；仅为需求基线，未实现。

| M7 候选 | 唯一 M2 查询行为 | 范围 |
|---|---|---|
| REP-01 | Q-01 | Sirchmunk HTTP 服务连接状态 |
| REP-02 | Q-02 | MCP 绑定与工具兼容状态 |
| REP-03 | Q-03 | 外部知识聚类与关系图谱 |
| REP-04 | Q-04 | 知识聚类列表、详情及后端已有统计 |
| REP-05 | Q-05 | Sirchmunk 自身历史，不与 DSH 历史合并 |
| REP-06 | Q-06 | 后端已有运行监控，不新增长期时序库 |
| REP-07 | Q-07 | 脱敏设置；首期仅允许修改非敏感 UI 设置 |

## 已确认范围

- 七项查询全部纳入首期，不新增插件报表数据库。
- 图谱支持 JSON、PNG、SVG 导出；首期不要求 CSV。
- 首期不新增 DSH 与 Sirchmunk Token 成本对比报表，不虚构节省比例。
- HTTP 管理状态与 MCP 检索状态分别展示。
- 不持续轮询；进入页面、明确成功的写操作后及手动刷新时读取。
- 来源、计算口径、分页和过滤能力须对照现有 API 核验，不因模型定义而假定后端已支持。
- 缺失字段显示未提供，不生成虚构置信度、用量、重启要求或历史记录。

## 阶段四已确认

不设置人工审批流；权限校验与破坏性操作二次确认仍然保留。

## 下一阶段

阶段六确认角色、行为权限、导出权限及既有 DSH 权限能力不足时的处理方式。
角色为业务职责，不代表 DSH 已有可直接复用的同名 RBAC 角色。不创建第二套账号系统。
图谱默认导出当前过滤视图仅为下一阶段待确认建议。

本记录为分阶段需求草稿，不代表最终需求规格说明书已完成或七模型 YAML 已生成。本轮未修改运行代码或连接配置。目标仍为 DSH 插件集成，不建设独立业务应用。
用户确认口径优先于此前草稿中不一致的建议。
